<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.blackducksoftware.integration.eclipse</groupId>
    <artifactId>blackduck-eclipse-releng</artifactId>
    <version>${blackduck.hub.eclipse.version}</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>blackduck-eclipse-update</artifactId>
  <packaging>eclipse-repository</packaging>

  <dependencies>
    <dependency>
      <groupId>com.blackducksoftware.integration.eclipse</groupId>
      <artifactId>com.blackducksoftware.integration.eclipse.plugin.maven-dependencies</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.eclipse.tycho.extras</groupId>
        <artifactId>tycho-eclipserun-plugin</artifactId>
        <version>${tycho.version}</version>
        <executions>
          <execution>
            <id>add-p2-composite-repository</id>
            <phase>package</phase>
            <goals>
              <goal>eclipse-run</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <applicationsArgs>
            <args>-application</args>
            <args>org.eclipse.ant.core.antRunner</args>
            <args>-buildfile</args>
            <args>int-p2composite.ant</args>
            <args>p2.composite.add</args>
            <args>-Droot.composite.label=${p2.repository.label}</args>
            <args>-Droot.composite.dir=${p2.repository.dir}</args>
            <args>-Dproject.build.directory=${project.build.directory}</args>
            <args>-DunqualifiedVersion=${unqualifiedVersion}</args>
          </applicationsArgs>
          <appArgLine>-DbuildQualifier=${buildQualifier}</appArgLine>
          <repositories>
            <repository>
              <id>oxygen</id>
              <layout>p2</layout>
              <url>http://download.eclipse.org/releases/oxygen</url>
            </repository>
          </repositories>
          <dependencies>
            <dependency>
              <artifactId>org.eclipse.ant.core</artifactId>
              <type>eclipse-plugin</type>
            </dependency>
            <dependency>
              <artifactId>org.apache.ant</artifactId>
              <type>eclipse-plugin</type>
            </dependency>
            <dependency>
              <artifactId>org.eclipse.equinox.p2.repository.tools</artifactId>
              <type>eclipse-plugin</type>
            </dependency>
            <dependency>
              <artifactId>org.eclipse.equinox.p2.core.feature</artifactId>
              <type>eclipse-feature</type>
            </dependency>
            <dependency>
              <artifactId>org.eclipse.equinox.p2.extras.feature</artifactId>
              <type>eclipse-feature</type>
            </dependency>
            <dependency>
              <artifactId>org.eclipse.equinox.ds</artifactId>
              <type>eclipse-plugin</type>
            </dependency>
          </dependencies>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-clean-plugin</artifactId>
        <executions>
          <execution>
            <id>default-clean-1</id>
            <goals>
              <goal>clean</goal>
            </goals>
            <configuration>
              <!-- This is a custom clean that Tycho implemented to avoid accumulating many versions in the target folder. -->
              <!-- Unfortunately, it does not play nice with Common Maven Parent (as it gets invoked constantly), so we should skip it. -->
              <skip>true</skip>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
